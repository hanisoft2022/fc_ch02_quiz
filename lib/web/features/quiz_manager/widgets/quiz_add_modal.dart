import 'package:flutter/material.dart';

class QuizAddModal extends StatelessWidget {
  const QuizAddModal({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 200, alignment: Alignment.center, child: Text('퀴즈 추가 폼 (여기에 구현)'));
  }
}
